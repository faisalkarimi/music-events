import React from "react";

const Search = () => {
  const styles = {
    fontSize: "18px",
    padding: "5px",
    width: "300px",
  };
  return <input type="text" placeholder="Search..." style={styles} />;
};

export default Search;
